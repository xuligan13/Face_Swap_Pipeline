# Face Swap Pipeline

Полностью автоматизированный пайплайн для замены лица: 2 референсных лица → множество сценовых изображений → генерация результата через Gemini и Seedream.

---

# 🚀 Установка

## 1. Клонирование репозитория
```bash
git clone https://github.com/xuligan13/Face_Swap_Pipeline
cd Face_Swap_Pipeline
```

## 2. Создание виртуального окружения
### macOS / Linux
```bash
python -m venv venv
source venv/bin/activate
```

### Windows
```bash
python -m venv venv
venv\Scripts\activate
```

## 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

---

# ⚙️ Конфигурация

## 1. Добавь свои API‑ключи

Создай файл **.env** в корне проекта:

```
GEMINI_API_KEY=your_key_here
SEEDREAM_API_KEY=your_key_here
```

---

## 2. Добавь изображения

### Папка FACE/
Положи **ровно 2 изображения лица**.  
Они будут использоваться для всех генераций.

### Папка SCENE/
Положи **20–25 изображений сцен** (можно больше).  
С каждой сценой будет выполняться отдельный пайплайн.

---

## 3. (Опционально) Измени промпт Gemini

Файл:

```
gemini_user_prompt.txt
```

Если хочешь — меняй под свои нужды.

---

# ▶️ Запуск программы

```bash
python main.py
```

Скрипт автоматически:

- Находит 2 лица  
- Находит все сцены  
- Показывает резюме  
- Ждёт нажатия **Enter**  
- Отправляет запросы: **Gemini → Seedream**  
- Сохраняет результаты в папку **OUTPUT/**

Имя файлов:

```
OUTPUT/0001_name.png
OUTPUT/0001_name.txt
```

---

# 🧪 Тестирование (перед выкладкой)

## 1. Mock‑режим Gemini (без использования ключа GEMINI)

В файле **config_example.json** установи:

```
"gemini": { "mode": "mock" }
```

Проверка:

- Положи ровно 2 лица → ошибок быть не должно  
- Положи 1 или 3 лица → программа должна сообщить о проблеме  
- Положи txt или битый файл в SCENE → программа должна игнорировать  

## 2. Тест реального API

- Запуск на 1 сцене  
- Проверить корректный ответ Gemini  
- Проверить, что Seedream возвращает изображение  
- Проверить сохранение в OUTPUT/

---

# 🔍 Улучшения (TODO)

- Добавить Web‑интерфейс (Gradio/FastAPI)  
- Мультипоточность для ускорения  
- Логирование в файл  
- Автоматическая оптимизация изображений (resize 1024×1024)  
- GitHub Actions (линтер + тесты)

---

# 🤝 Contributing

PR‑ы приветствуются!  
Пожалуйста, **не загружайте .env** и приватные изображения других людей.

 🎭 Face Swap Pipeline  
**Автоматический пайплайн подмены лица на основе двух face-референсов, сценового изображения, Gemini и Seedream.**

Этот проект позволяет загружать две фотографии лица + множество сцен и автоматически генерировать итоговые изображения, где лицо с исходных фотографий переносится на персонажа из сцены.  
Сначала запрос уходит в **Google Gemini**, который создаёт идеальный промпт, затем изображение генерируется через **Seedream**.

---

# 🚀 Возможности

✔ Использует **2 изображения лица** как референсы идентичности  
✔ Автоматически перебирает **все сцены** в папке  
✔ Генерация промпта через **Google Gemini (официальная библиотека)**  
✔ Генерация результата через **Seedream 4.0**  
✔ Сохранение результата в PNG  
✔ Поддержка mock-режима  
✔ Интерактивный CLI — подтверждение перед запуском  
✔ Логи + обработка ошибок  
✔ Удобная структура проекта

---

# 📁 Структура проекта

Face_Swap_Pipeline/
│
├── autodataset_tool/
│ └── examples/
│ ├── Face1.jpeg
│ ├── Face2.png
│ ├── Scene.jpg
│ └── output.jpeg
│
├── FACE/
├── SCENE/
├── OUTPUT/
│
├── main.py
├── gemini_client.py
├── seedream_client.py
├── utils.py
│
├── gemini_user_prompt.txt
├── requirements.txt
├── README.md
└── .env.example

yaml
Skopiuj kod

---

# 🧠 Before / After (пример)

Добавь ваши файлы в директорию:

autodataset_tool/examples/

yaml
Skopiuj kod

После этого нижняя таблица автоматически покажет изображения:

| Face 1 | Face 2 | Scene | Result |
|--------|--------|--------|---------|
| ![](autodataset_tool/examples/Face1.jpeg) | ![](autodataset_tool/examples/Face2.png) | ![](autodataset_tool/examples/Scene.jpg) | ![](autodataset_tool/examples/output.jpeg) |

---

# 🛠 Установка

## 1. Клонируйте репозиторий

```bash
git clone https://github.com/xuligan13/Face_Swap_Pipeline
cd Face_Swap_Pipeline
2. Создайте виртуальное окружение
bash
Skopiuj kod
python -m venv venv
source venv/bin/activate        # macOS/Linux
venv\Scripts\activate           # Windows
3. Установите зависимости
bash
Skopiuj kod
pip install -r requirements.txt
🔐 Настройка
Создайте файл .env (или скопируйте .env.example):

ini
Skopiuj kod
GEMINI_API_KEY=ваш_ключ
SEEDREAM_API_KEY=ваш_ключ
GROQ_API_KEY=ваш_ключ_если_используется
📸 Подготовка данных
Папка FACE
Поместите туда ровно 2 фотографии лица.

Папка SCENE
Поместите туда любые изображения сцен, например:

улица

пляж

кафе

интерьер

позы персонажа

Система сама переберёт их по очереди.

▶️ Запуск
bash
Skopiuj kod
python main.py
Скрипт:

отображает найденные face-изображения

отображает список сцен

ждёт нажатия Enter

для каждой сцены:

отправляет запрос в Gemini

получает promt

отправляет запрос в Seedream

сохраняет PNG результат в OUTPUT/

🔌 Использование официальной библиотеки Gemini
В проекте используется библиотека:

Skopiuj kod
google-generativeai
Документация:
https://ai.google.dev/gemini-api/docs/get-started/python?hl=ru

🧪 Тестирование проекта перед загрузкой на GitHub
✔ 1. Проверка mock-режима
Включите mock в config, убедитесь, что пайплайн запускается без ключей.

✔ 2. Проверка face-папки
Попробуйте добавить меньше или больше двух изображений — программа должна выдавать понятную ошибку.

✔ 3. Проверка SCENE-папки
Добавьте 1–2 сцены и проверьте, что создаются PNG.

✔ 4. Проверка Gemini
Убедитесь, что ответ содержит promt (выводится в консоль).

✔ 5. Проверка Seedream
Файл PNG должен появиться в OUTPUT/.

✔ 6. Проверка README
Положите изображения в autodataset_tool/examples/ и убедитесь, что таблица отображает их.

🤝 Contributing
Pull-request приветствуются.
Файлы .env и любые ключи должны быть исключены .gitignore.
